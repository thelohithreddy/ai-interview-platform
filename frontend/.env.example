# Frontend environment variables
# Use 5001 (not 5000) — macOS AirPlay often hijacks port 5000 and returns HTTP 403
VITE_API_URL=http://127.0.0.1:5001

# OAuth redirect URIs must match backend/.env BACKEND_URL, e.g.:
# http://127.0.0.1:5001/api/auth/google/callback
